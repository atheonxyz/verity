# Verity SDK — ProGuard rules for library builds.
# consumer-rules.pro handles rules for downstream consumers.
# This file is for the library's own minification (if enabled).

-keepattributes *Annotation*
